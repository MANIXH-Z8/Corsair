# Product requirements

A non-technical user describes a prediction problem, approves a machine-readable project brief, supplies historical data or downloads a template, and receives a reproducible classification or regression comparison.

Required path: discovery → explicit approval → CSV/XLSX profile → training-ready check → run → report. The MVP excludes connectors, web retrieval, forecasting, clustering, NLP, computer vision, multi-tenancy, and live model serving.
